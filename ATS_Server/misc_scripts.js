function standardScoring(playerObj){
	var scoreModifier = 0;
	// gain 1VP for each EMPTY power reactor (count no of PR_0, MR_0, ER_0, VR_0)
	// gain 1VP for every THREE coins remaining
	// for card in grid, if card = end game type, run endgame function for each card
	return scoreModifier;
}

function ObjectiveModifier(playerObj, ObjectiveList){
	return null;
}

